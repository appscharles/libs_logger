sourceSets {
    main {
        resources {
            srcDirs = ["src/main/java", "src/main/resources"]
            includes = ["**/*.txt", "**/*.fxml", "**/*.css", "**/*.png", "**/*.gif", "**/*.properties", "**/*.html", "**/*.sql", "**/*.xml"]
        }
    }
}